# Work Handoff Area

This folder is for temporary local handoff notes such as `.agent/work/current.md`.

Recommended `.gitignore` entry:

```gitignore
.agent/work/*
!.agent/work/README.md
```

Do not store durable project truth here. Promote durable decisions to `.agent/decisions/` or the relevant `.agent/domains/*` file.

