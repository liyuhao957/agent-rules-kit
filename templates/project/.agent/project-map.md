# Project Map

This file is generated or refreshed by:

```bash
python3 scripts/bootstrap-project-context.py
```

It should contain only facts that can be rediscovered from current code/config, plus clearly marked candidates. Treat it as a map, not proof.

Run bootstrap again after large structure changes.

